---
- tgt: write
- init:
  commands:
  - launch loader out JSON stdout out bytes stderr program files2mapped offset_min.json omin offset_max.json omax radius_min.json rmin radius_max.json rmax
  - launch generate out JSON stdout out bytes stderr in JSON stdin program generatechanges
  - launch render out JSON stdout out bytes stderr in JSON stdin program renderchanges
  - launch color out JSON stdout out bytes stderr in JSON stdin program pseudocolor
  - launch write out bytes stderr in JSON stdin program writeimage
  - wait_data omin omax rmin rmax
- generate: init
  commands:
  - [feed, generate, direct, 1000000, count, input, rmax, radius_max, input, rmin, radius_min, input, omax, offset_max, input, omin, offset_min]
  - close generate
  - wait_data changes
- render: generate
  commands:
  - [feed, render, direct, 1000, size, input, changes, changes]
  - close render
  - wait_data heightfield
- convert: render
  commands:
  - feed color input heightfield map
  - close color
  - wait_data image
- write: convert
  commands:
  - feed write direct radius.tiff filename input image image
  - close write
